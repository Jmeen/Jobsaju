export interface BadgeProps { children: React.ReactNode; tone?: 'muted' | 'brand'; }
/**
 * @startingPoint section="Core" subtitle="필 배지 — 태그·마퀴 칩에 사용" viewport="700x100"
 */
export function Badge(props: BadgeProps): JSX.Element;

# Character source assets

고해상도 PNG 원본과 생성 과정의 샘플을 보관하는 폴더입니다. 이 폴더는 Vite의 `public` 경로 밖에 있으므로 배포 산출물에 포함되지 않습니다.

배포용 이미지는 `python scripts/optimize_characters.py`로 생성하며, 결과는 `public/characters/optimized/*.webp`에 저장됩니다.
